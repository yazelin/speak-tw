---
name: speak-tw
description: 講人話檢查。用程式掃出中文寫作的老毛病（假對比句型、禁用詞、AI 味、半形標點、簡體字、emoji），交稿前跑一次。任何 agent 都能用，因為它只是一支會回傳退出碼的 CLI。
---

# speak-tw：講人話

寫完對外文字（README 中文段、網頁文案、blog、貼文）之後，**跑這支再交**。

```bash
~/speak-tw/bin/speak-tw --public <檔案或目錄>
```

有問題會 exit 1 並逐條列出位置。改完再跑一次，直到乾淨。

## 什麼時候要跑

- 對外頁面、README、blog、FB 貼文、課程講義 —— 一律加 `--public` <!-- speak-tw-ok 這一行在示範規則本身 -->
- 內部筆記（commit、NEXT.md、spec） —— 不加 `--public`，只擋 any 級的規則 <!-- speak-tw-ok 這一行在示範規則本身 -->

## 放行

確定某一處是刻意的，在那一行加 `speak-tw-ok`（HTML 用 `<!-- speak-tw-ok -->`）。
**要放行就要明講**，不能默默略過。

## 為什麼是程式不是 prompt

prompt 型的規範要靠「想到要用它」。同一種毛病被指正五次都沒改掉，就是這樣漏的。
一支會回傳退出碼的程式不需要被想起來 —— 放進測試流程就永遠會跑。 <!-- speak-tw-ok 這一行在示範規則本身 -->

## 規則從哪來

主要來自使用者實際指正過的案例（見 `rules.mjs` 每條的 `bad` / `good`）。
每條規則都被測試逐條驗過：抓得到壞例、放得過好例、而且不咬到別條的好例。
